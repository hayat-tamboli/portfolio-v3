---
import Navbar from "@/components/Navbar.astro";
import Layout from "../layouts/Layout.astro";
import Image from "astro/components/Image.astro";
import hayat_profile from "@/Images/hayat_profile.png";
import solapur from "@/Images/Solapur_Schematic_map.png";
---

<Layout title="Hayat Tamboli - about me">
  <main
    class="bg-slate-100 min-h-screen flex flex-col justify-center items-center px-4 md:px-16 lg:px-32"
  >
    <div class="md:w-3/5 px-4 my-40">
      <div class="flex flex-wrap items-start">
        <div class="gallery-container">
          <!-- Image containers where images will be added dynamically -->
          <!-- <div class="image-container">
            <div
              class="bg-white p-3 shadow-lg shadow-slate-300/30 md:absolute md:rotate-1"
              id="hayat-photo"
            >
              <Image
                src={hayat_profile}
                alt="Hayat's photo"
                class="aspect-square object-cover"
              />
              <p
                class="handwritten m-3 -rotate-2 text-center text-2xl opacity-70 uppercase"
              >
                That's me :)
              </p>
            </div>
          </div> -->
          <!-- <div class="image-container">
            <div
              class="bg-white p-3 shadow-lg shadow-slate-300/30 md:absolute md:rotate-1"
              id="hayat-photo"
            >
              <Image
                src={hayat_profile}
                alt="Hayat's photo"
                class="aspect-square object-cover"
              />
              <p
                class="handwritten m-3 -rotate-2 text-center text-2xl opacity-70 uppercase"
              >
                That's me :)
              </p>
            </div>
          </div> -->
          <!-- <div class="image-container">
            <div
              class="bg-white p-3 shadow-lg shadow-slate-300/30 md:absolute md:rotate-1"
              id="hayat-photo"
            >
              <Image
                src={hayat_profile}
                alt="Hayat's photo"
                class="aspect-square object-cover"
              />
              <p
                class="handwritten m-3 -rotate-2 text-center text-2xl opacity-70 uppercase"
              >
                That's me :)
              </p>
            </div>
          </div> -->
          <!-- <div class="image-container">
            <div
              class="bg-white p-3 shadow-lg shadow-slate-300/30 md:absolute md:rotate-1"
              id="hayat-photo"
            >
              <Image
                src={hayat_profile}
                alt="Hayat's photo"
                class="aspect-square object-cover"
              />
              <p
                class="handwritten m-3 -rotate-2 text-center text-2xl opacity-70 uppercase"
              >
                That's me :)
              </p>
            </div>
          </div> -->
        </div>
        <div
          class="md:relative md:w-1/3"
          id="photos"
          onmouseover="document.getElementById('hayat-photo').classList.add('hidden-m')"
          onmouseout="document.getElementById('hayat-photo').classList.remove('hidden-m')"
        >
          <div
            class="bg-white p-3 shadow-lg shadow-slate-300/30 md:absolute rotate-6 hidden md:block"
            id="map-photo"
          >
            <Image
              src={solapur}
              alt="Hayat's photo"
              class="aspect-square object-cover"
            />
            <p
              class="handwritten m-3 -rotate-2 text-center text-1xl opacity-70 uppercase"
            >
              Solapur - My Home town
            </p>
          </div>
          <div
            class="bg-white p-3 shadow-lg shadow-slate-300/30 md:absolute md:rotate-1"
            id="hayat-photo"
          >
            <Image
              src={hayat_profile}
              alt="Hayat's photo"
              class="aspect-square object-cover"
            />
            <p
              class="handwritten m-3 -rotate-2 text-center text-2xl opacity-70 uppercase"
            >
              That's me :)
            </p>
          </div>
        </div>
        <div
          class="rounded-3xl border-dashed flex flex-col space-y-4 justify-center min-w-40 md:w-2/3 md:p-8 pt-4"
        >
          <p class="text-2xl text-slate-950/90">
            Hi, I am Hayat, an Interaction Designer with a knack for exploring
            new technologies. Currently pursuing Master of Design at <a
              href="https://www.idc.iitb.ac.in/"
              class="font-bold hover:decoration-black hover:decoration-wavy hover:underline duration-250 transition-all"
            >
              IDC, IIT Bombay</a
            >.
          </p>
          <p class="text-2xl text-slate-950/90 mt-4">
            When not designing or coding, you will find me tinkering with
            electronics, playing NFS and watching Formula 1.
          </p>
        </div>
      </div>
    </div>
  </main>
</Layout>

<style>
  /* #hayat-photo {
    transition: all 0.5s ease;
    z-index: 1;
  }

  #hayat-photo.hidden-m {
    transform: translateX(-100%);
    z-index: -1;
  } */

  .image-container {
    width: 400px;
    position: absolute;
    top: 10em;
    right: 10em;
  }
  .image {
    border-radius: 2em;
    width: 100%;
    height: 100%;
    border: 15px solid white;
    cursor: pointer;
  }
  .image-container:first-child {
    transform: rotate(23deg);
  }
  .image-container:nth-child(2) {
    transform: rotate(328deg);
  }
  .image-container:nth-child(3) {
    transform: rotate(169deg);
  }
</style>

<script lang="ts">
  //Get all image container
  let childElement = document.querySelectorAll(".image-container");

  //Array of image URLs
  let imageUrls = ["image-1.jpg", "image-2.jpg", "image-3.jpg", "image-4.jpg"];

  //Initial z-index value
  let zIndex = 1;

  //Iterate through each image container
  childElement.forEach((element, index) => {
    //Create an image element
    let img = document.createElement("img");
    img.setAttribute("src", imageUrls[index]);
    img.setAttribute("class", "image");
    element.appendChild(img);

    //Add a click event listener to each image container
    element.addEventListener("click", () => {
      zIndex++;
      element.style.right = "50em";
      element.style.zIndex = zIndex.toString();
      element.style.transform = "rotate(0deg)";

      //Apply transition to right property
      element.style.transition = " right 0.3s ease";

      //Remomve the class and reset properties after a delay
      setTimeout(() => {
        zIndex -= 3;
        element.style.right = "";
        element.style.zIndex = zIndex.toString();
        element.style.transform = "";

        //Set a transition for clearing the transition property
        element.style.transition = "all 0.3s ease";

        //Clear the transition property after a slight delay
        setTimeout(() => {
          element.style.transition = "";
        }, 300);
      }, 1000);
    });
  });
</script>
